function randomjoke() {
  return "You are not funny";
}

export default randomjoke;

//what to put in gitignore

//node_modules  - this is where all the packages are installed  - you don't want to push this to github because it's too big  - you can always reinstall the packages by running npm install  - you can also run npm install to install all the packages in the package.json file   - you can also run npm install --save to install a package and add it to the package.json file

//how to
